using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class _ControladorDeJogo : MonoBehaviour
{

    public      int             teste;
    public      string[]        tiposDano;
    public      GameObject[]    efeitos_dano;
    public      GameObject      efeitos_morte;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
